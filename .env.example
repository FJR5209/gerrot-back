# Exemplo de variáveis de ambiente. NÃO comitar credenciais reais.

# Server
PORT=3000
NODE_ENV=production

# Database (MongoDB Atlas) - exemplo: mongodb+srv://user:password@cluster0.xyz.mongodb.net/dbname
DATABASE_TYPE=mongodb
DATABASE_URL=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/gerrot?retryWrites=true&w=majority
DATABASE_NAME=gerrot

# JWT
JWT_SECRET=troque-esta-chave-por-uma-segura
JWT_EXPIRES_IN=7d

# Redis - preferível usar REDIS_URL para serviços gerenciados (Upstash)
# Exemplo Upstash (TCP): REDIS_URL=rediss://:<password>@<host>:<port>
REDIS_URL=
# Ou em host/port/password (se preferir):
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TLS=false

# Google Gemini (se usar)
GEMINI_API_KEY=
GEMINI_MODEL=models/gemini-2.5-flash

# AWS (opcional)
AWS_S3_BUCKET_NAME=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# CORS
FRONTEND_URL=

# Observações:
# - Para Upstash: crie a instância e copie a URL TCP (rediss://... ou redis://...).
# - Não deixe credenciais no repositório público. Use secrets no AWS App Runner.
